# mlb-player-predictions

The code behind the Fantasy Toolz system for MLB projections.

## Quickstart

### Hitting

The workhorse code is `run_batting_projections.py`. The first step is to make sure all the data we will need is up to date; this can be done using the `stat-scraping` library provided.
